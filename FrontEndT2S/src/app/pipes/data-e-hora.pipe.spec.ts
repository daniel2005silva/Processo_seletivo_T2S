import { DataEHoraPipe } from './data-e-hora.pipe';

describe('DataEHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new DataEHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
